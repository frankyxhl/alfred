# REF-2213: Evolve-Run-20260419-094124

**Applies to:** FXA project
**Last updated:** 2026-04-19
**Last reviewed:** 2026-04-19
**Status:** Active

---

## What Is It?

Run log for FXA-2149 (Evolve-CLI) second execution on 2026-04-19 09:41:24. Records collected
signals, generated candidates (including the FXA-2212 DAG-graph seed), evaluator scores, and the
implementation chosen (if any).

---

## Content

### Phase 0–1: Setup

- Open evolve PR check: PASS (none open; PR #52 and #53 already merged)
- Evolution Philosophy loaded: FXA-2146 (threshold 7.0, review ≥ 9.0, Evolve-CLI weights TV35/SR30/BC20/Nec15)
- Run log: FXA-2213
- Seed carried over from previous session: **FXA-2212** (ASCII DAG graph layout — user-requested)

### Phase 2: Signals

| Signal           | Result                                                |
|------------------|-------------------------------------------------------|
| pytest           | 663 passed / 0 failed (unchanged from post-PR-52)     |
| ruff check       | 0 issues                                              |
| coverage (TOTAL) | 96% (2415 stmts, 85 missed — down from 90 pre-PR-52)  |
| af validate      | 228/228 docs valid                                    |

Top miss-density modules (post-PR-52):

| File                      | Missed | Notes                                                                                                                                                             |
|---------------------------|--------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| commands/create_cmd.py    |     22 | Spec-mode error paths (unchanged).                                                                                                                                |
| commands/fmt_cmd.py       |     20 | doc_type=None + exception arms (unchanged).                                                                                                                       |
| commands/update_cmd.py    |     10 | Spec-mode + rename arms (unchanged).                                                                                                                              |
| core/ascii_graph.py       |     10 | Malformed-loop guards + inline-fallback paths (unchanged).                                                                                                        |
| commands/validate_cmd.py  |      9 | **Lines 131–133 `if not lines` appears unreachable** (`content.split("\n")` always returns ≥1 element). Lines 179–180, 195–196 are `except ValueError` dead arms. |
| commands/plan_cmd.py      |      5 | "no Steps section found" fallback + invalid workflow metadata arms.                                                                                               |
| core/scanner.py           |      3 | PKG iterdir non-file + NotADirectory/FileNotFound.                                                                                                                |
| core/parser.py            |      2 | "Change History heading but no table" fallback (line 194) + trailing append (line 284).                                                                           |
| core/compose.py           |      2 | Empty-edge guards.                                                                                                                                                |
| commands/where_cmd.py     |      2 | Non-Path Traversable (zip install — environment-dependent).                                                                                                       |

### Phase 3: Candidates (Generator role)

| ID | Target                             | Change                                                                                       | Evidence                                               |
|----|------------------------------------|----------------------------------------------------------------------------------------------|--------------------------------------------------------|
| S1 | `core/dag_graph.py` (new)          | Opt-in `--graph-layout=dag` ASCII DAG renderer (fork/join/cross-phase tracks)                | FXA-2212 seed (user feedback on PR #52)                |
| C4 | `commands/create_cmd.py:46–48`     | Coerce `area` to `str` in `_next_acid_in_area` so `area == "00"` is robust to int input      | Brittleness observation (carry-over from FXA-2209 run) |
| N1 | `commands/validate_cmd.py:131–133` | Remove dead branch `if not lines` — `content.split("\n")` always yields ≥1 element           | Dead code; minimization                                |
| N2 | `tests/test_plan_cmd.py`           | Add test for `plan_cmd.py:276` (raw-section-text fallback when `## Steps` has no numbered items) | coverage gap                                           |
| N3 | `tests/test_parser.py`             | Add test for `parser.py:194` (Change History heading but no table — early return arm)        | coverage gap                                           |

### Phase 4: Evaluator (skeptical)

Weights: TV 35% / SR 30% / BC 20% / Nec 15%.

| ID | TV | SR | BC | Nec | Weighted | Verdict         |
|----|---:|---:|---:|----:|---------:|-----------------|
| S1 |  6 |  5 | 10 | 3   | 6.05     | DISCARD (< 7.0) |
| C4 |  6 |  9 | 10 | 2   | 7.10     | KEEP (R1) → DISCARD (R2, broken fix) |
| N1 |  5 |  9 | 10 | 3   | 6.90     | DISCARD         |
| N2 |  9 | 10 | 10 | 4   | 8.75     | KEEP            |
| N3 |  8 | 10 | 10 | 3   | 8.25     | KEEP            |

**Reasoning:**

- **S1 (FXA-2212)** — New module (~200+ lines est), new flag, new test file. TV dragged down by fiddly
  ASCII corner-char alignment across widths. SR=5 because scope is much larger than a typical evolve
  output. Nec=3 because no defect signal — pure ergonomic request. **Per FXA-2212 REF policy: REF
  remains; no promotion on first discard.**
- **C4** — Scored 7.10 at Phase 4 (PRP R1). During trinity PRP review (R1 Codex 6.1 / Gemini 8.9
  both FIX), empirically discovered the proposed one-line fix is broken: `str(0) == "0"` (not
  `"00"`), so the `area == "00"` check still fails and `_next_acid_in_area` still returns the
  reserved `"0000"` ACID. Any correct repair (`str(area).zfill(2)` or restructure to
  `area_int == 0`) either yields dead code under FXA-2208's pyright gate or widens the public
  type annotation (scope creep). **C4 re-discarded at PRP R2.** The underlying brittleness
  observation is preserved here for a future run: if a caller contract change ever legitimately
  routes int area values to `_next_acid_in_area`, that caller work carries C4 as a prerequisite.
- **N1** — Deletion closes the coverage gap by removing the arm, but the arm is defensive. If upstream
  `str.split` semantics ever change, the guard would become load-bearing. TV=5 because the only test
  is full-suite passage after deletion.
- **N2** — CliRunner-based test with clear `--todo`-output assertion. Pins the raw-section-text
  fallback: when a SOP's `## Steps` section has content but no recognized `1. 2. 3.` numbered
  items, the TODO emits `{phase}.1 [{sop_id}] {steps_section.strip()}`. Original target (line
  600 `if not todo_items: return`) was dropped after review revealed it's unreachable — every
  `_build_todo_items` path returns at least one item.
- **N3** — Unit test on parser directly; pins the documented "no table" fallback behaviour.

### Phase 4 (cont.): Top candidate / bundle selection

- **Strict top candidate:** N2 at 8.75.
- **Viable bundle (Run 1 pattern):** N2 + N3 + C4 = three small changes, all ≥ 7.0, each in a
  different module. C4 is the only source change; N2/N3 are test-only. Composes as a single CHG
  without atomicity violations (all three harden observable behaviour at documented edges).

### Phase 4 (cont.): R2 bundle reduction — N2 + N3 only

PRP R1 review caught that C4's proposed fix is broken. Dropped C4 for R2; shipping **N2 + N3**
(both test-only). Run log reflects this — C4 row verdict reads "KEEP (R1) → DISCARD (R2,
broken fix)".

### Phase 5: Issue + branch + PRP

- GitHub issue: #54
- Branch: `chore/54-evolve-cli-20260419-run2`
- PRP: FXA-2214
- PRP review (COR-1602 strict):
  - R1: Codex 6.1 FIX / Gemini 8.9 FIX — both caught C4 `str(area)` fix is broken (empirically: `str(0) == "0"`, not `"00"`, so the `area == "00"` check still fails) and C4's type-annotation conflict with FXA-2208 pyright gate
  - R2 (C4 dropped): Codex 9.8 PASS / Gemini 9.9 PASS

### Phase 6: CHG + TDD

- CHG: FXA-2215
- TDD Red (mutation verification):
  1. `plan_cmd.py:276` → emit `"MUTATED"` instead of `steps_section.strip()` → `test_plan_todo_raw_section_text_fallback` FAILED (assert missing "TODO: fill in…") → reverted → GREEN
  2. `parser.py:192` → `if table_header_end is None` → `if False` → `test_parse_metadata_change_history_heading_without_table` FAILED (TypeError: unsupported + on None) → reverted → GREEN
- TDD Green: both tests pass on unmutated source
- Refactor: Gemini advisory applied — hoisted `parse_metadata` import to module top in `test_parser.py` for consistency with existing `H1_PATTERN` import
- Hard gate: `pytest 665/665 pass`, `ruff check 0 issues`, `af validate 229/229 valid`
- Coverage: 85 → 83 missed lines (2 closed). `plan_cmd.py` 98% → 99%; `parser.py` still 99% but line 194 now covered (remaining single miss = line 284).
- Code review (COR-1610):
  - Codex: 9.8 PASS
  - Gemini: 9.9 PASS (advisory: hoist import — applied)

### Phase 4 (cont.): Notes on the FXA-2212 discard

- FXA-2212 (the DAG-graph seed that Frank specifically asked me to bring into this run) scored
  **6.05**, below the 7.0 threshold.
- Breakdown: **Scope restraint (SR=5)** and **Necessity (Nec=3)** were the two load-bearing
  deductions. Scope is large (new module + new flag + new test file — not a small test addition).
  Necessity is low because no code-layer signal forces the change (tests pass, ruff clean, linear
  layout works as documented); it's a pure user-ergonomics request.
- Per FXA-2212's own policy ("Stay as REF until the next Evolve-CLI run; let the Evaluator score
  it cold. If discarded twice, bump to PRP"): this is the **first** cold pass. REF remains. Expected
  action: defer; try again next run; on second discard, promote to PRP and drive manually.
- **This is the Evaluator working correctly** — FXA-2212 is a PRP-class change in disguise (opt-in
  feature, new module). The rubric is nudging it toward the PRP path rather than stapling it into
  an evolve PR.

### Selected output (pending user decision)

- **Default:** bundle N2 + N3 + C4 (top three passing candidates).
- **Alternative A:** single-candidate ship N2 (8.60) for maximum scope restraint.
- **Alternative B:** no-op — the landscape is clean and the two top candidates both sit below 9.0,
  signalling diminishing returns.
- **Alternative C:** override the Evaluator and ship FXA-2212 as a PRP manually (not an evolve
  output — acknowledge this breaks the rubric).

---

## Change History

| Date       | Change                                                                                    | By             |
|------------|-------------------------------------------------------------------------------------------|----------------|
| 2026-04-19 | Initial version                                                                           | —              |
| 2026-04-19 | Phase 0–4 recorded: signals, candidates (incl. FXA-2212 seed), scores, selection proposal | Frank + Claude |
