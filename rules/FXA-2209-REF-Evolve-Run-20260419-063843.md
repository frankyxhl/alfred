# REF-2209: Evolve-Run-20260419-063843

**Applies to:** FXA project
**Last updated:** 2026-04-19
**Last reviewed:** 2026-04-19
**Status:** Active

---

## What Is It?

Run log for FXA-2149 (Evolve-CLI) execution on 2026-04-19 06:38:43. Records collected signals, generated improvement candidates, evaluator scores, and the implementation chosen (if any).

---

## Content

### Phase 0–1: Setup

- Open evolve PR check: PASS (none open)
- Evolution Philosophy loaded: FXA-2146
  - Discard threshold: < 7.0
  - Review pass: ≥ 9.0
  - Evolve-CLI weights: Test verifiability 35% / Scope restraint 30% / Backward compatibility 20% / Necessity 15%
- Run log: FXA-2209 (this file)

### Phase 2: Signals

| Signal           | Result                                                                                  |
|------------------|-----------------------------------------------------------------------------------------|
| pytest           | 660 passed / 0 failed                                                                   |
| ruff check       | 0 issues                                                                                |
| coverage (TOTAL) | 96% (2415 stmts, 90 missed)                                                             |
| af validate      | 224/224 docs valid (0 issues)                                                           |

Top miss-density modules:

| File                            | Missed | Notable uncovered ranges                                          |
|---------------------------------|--------|-------------------------------------------------------------------|
| commands/create_cmd.py          | 22     | spec-mode error paths (315–342), CLI-mode error paths (424, 435)  |
| commands/fmt_cmd.py             | 20     | doc_type=None guards (44), exception arms (380–394, 399–400)      |
| commands/validate_cmd.py        | 11     | malformed-history-table arms (60, 70), empty-doc/Tags edge (132)  |
| core/ascii_graph.py             | 10     | malformed-loop guards (165–168), inline-fallback paths (270, 307) |
| commands/update_cmd.py          | 10     | spec-mode + rename-collision arms                                 |
| commands/plan_cmd.py            |  5     | "no Steps section" + invalid-workflow-metadata arms               |
| core/scanner.py                 |  3     | PKG iterdir non-file skip (55), NotADirectory/FileNotFound (64–65)|
| core/parser.py                  |  2     | (194, 284)                                                        |
| core/compose.py                 |  2     | empty-edge guards (209, 214)                                      |
| commands/where_cmd.py           |  2     | non-Path Traversable + missing-file arm (26, 29)                  |
| commands/_helpers.py            |  2     | atomic_write cleanup arm (86–87)                                  |
| commands/list_cmd.py            |  1     | empty-result JSON `[]` echo (66)                                  |

### Phase 2 (cont.): Source observations

1. **create_cmd.py duplication (Mode 1 spec vs Mode 2 CLI):** Both branches re-implement the same validations
   (prefix format, ACID format, ACID=0000 reservation, area auto-assign, duplicate-collision check) in slightly
   different order. Mode 1 lines 314–337 duplicate Mode 2 lines 324–336 + 439–440. Cleanup candidate.
2. **`_next_acid_in_area` area-00 special case:** `start = area_int * 100 + (1 if area == "00" else 0)` —
   string compare on `area`. Brittle if upstream ever passes int. Minor.
3. **`fmt_cmd.normalize_metadata_order` line 44:** `if doc_type is None: return False` — only reachable when
   the doc has an unknown type_code (no DocType enum match). Untested; tiny.
4. **`atomic_write` cleanup arm uncovered.** No test exercises the failure-then-cleanup branch.
5. **`list_cmd` `--json` empty result** uncovered (line 66 — `click.echo("[]")`).
6. **All other gaps are guard arms** — most are environment-dependent (zip-installed package, filesystem
   races) or defensive type-narrowing. Low-priority.

### Phase 2 (cont.): SOP-vs-code gap

- Compared SOP corpus (60 SOPs) against CLI surface. No structural drift detected:
  CLI commands all have SOP coverage; SOP step references match CLI command/flag names.

### Phase 3: Candidates (Generator role)

| ID | Target                                  | Change                                                                                              | Evidence                                                |
|----|-----------------------------------------|-----------------------------------------------------------------------------------------------------|---------------------------------------------------------|
| C1 | `tests/test_list_cmd.py`                | Add test for `af list --json` returning `[]` when filters match nothing                             | list_cmd.py:66 uncovered                                |
| C2 | `tests/test_helpers.py`                 | Add test for `atomic_write` cleanup-after-failure arm                                               | _helpers.py:86–87 uncovered                             |
| C3 | `commands/create_cmd.py`                | Extract shared `_resolve_create_inputs()` helper to dedup Mode 1 spec / Mode 2 CLI validation       | ~80 lines duplicated; create_cmd has highest miss count |
| C4 | `commands/create_cmd.py`                | Coerce `area` to str in `_next_acid_in_area` so `area == "00"` check is robust to int input        | brittleness observation                                 |
| C5 | `tests/test_validate_cmd.py`            | Add tests for malformed Change-History header arms (lines 60, 70)                                   | validate_cmd.py:60,70 uncovered                         |

### Phase 4: Evaluator (skeptical)

Weights: Test verifiability 35% / Scope restraint 30% / Backward compatibility 20% / Necessity 15%.

| ID | TV (35%) | SR (30%) | BC (20%) | Nec (15%) | Weighted | Verdict         |
|----|---------:|---------:|---------:|----------:|---------:|-----------------|
| C1 | 10       | 10       | 10       | 5         | 9.25     | KEEP            |
| C2 | 10       | 10       | 10       | 2         | 8.80     | KEEP            |
| C3 | 7        | 4        | 7        | 4         | 5.65     | DISCARD (< 7.0) |
| C4 | 6        | 9        | 10       | 2         | 7.10     | KEEP            |
| C5 | 9        | 10       | 10       | 3         | 8.60     | KEEP            |

**Reasoning:**
- **C3 discarded:** large refactor (~80 lines moved), high cascade risk into both create modes; necessity is
  weak — duplication exists but is fully covered by behavior tests, no defect symptom. Fails the "Compression
  as Intelligence" test on the necessity dimension (no concrete defect/lint/coverage signal forces this).
- **C1, C2, C5:** narrow, test-only additions; high test verifiability; zero CLI surface change; necessity
  capped at 5–6 because each closes only 1–3 missed lines (low impact).
- **C4:** tiny one-line robustness fix with a regression test; no observable defect today, hence Nec=3.
  Passes threshold but borderline.

### Phase 4 (cont.): Top candidate selection

Four candidates passed threshold (C1=9.25, C2=8.80, C5=8.60, C4=7.10). C3 discarded.

**User-selected bundle (option 2):** C1 + C2 + C5 — three test-only additions closing the highest-confidence
coverage gaps. Each individually passes threshold and all three share the same change type (tests, no
source change), so they compose into a single PRP/CHG without violating atomicity. C4 (one-line source
robustness tweak) is deferred — merging it with test-only bundle would mix change types and weaken scope
restraint.

**Change title:** "Close three edge-arm coverage gaps with tests"

### Phase 5: Issue + branch + PRP

- GitHub issue: #51
- Branch: `chore/51-evolve-cli-20260419`
- PRP: FXA-2210
- PRP review (COR-1602 strict):
  - R1: Codex 9.1 PASS / Gemini 7.7 FIX — both flagged Gap 3 diagnostic misquote
  - R2: Codex 9.8 PASS / Gemini 10.0 PASS
  - R3 (self-caught): Codex 9.9 PASS / Gemini 10.0 PASS — Gap 2 rescoped to double-failure after discovering existing test covered single-failure path

### Phase 6: CHG + TDD

- CHG: FXA-2211
- TDD Red (mutation verification):
  1. `list_cmd.py:66` → `click.echo("MUTATED")` → `test_list_json_empty_result` FAILED → reverted → GREEN
  2. `_helpers.py:88` → `raise` (unlink error) inside inner except → `test_atomic_write_double_failure_preserves_original_error` FAILED → reverted → GREEN
  3. `validate_cmd.py:60` → `return []` → `test_validate_history_header_early_return_arms` FAILED → reverted → GREEN
- TDD Green: all three tests pass on unmutated source
- Refactor: none
- Hard gate: `pytest 663/663 pass`, `ruff check 0 issues`, `af validate 226/226 valid`
- Coverage: 90 → 85 missed lines (5 closed). `_helpers.py` now 100%, `list_cmd.py` now 100%, `validate_cmd.py` 94% (from 93%).
- Code review (COR-1610):
  - Codex: 9.8 PASS
  - Gemini: 10.0 PASS
- Post-review advisory (non-blocking) applied: added explicit `mock_unlink.assert_called_once()` assertion in double-failure test

---

## Change History

| Date | Change | By |
|------|--------|----|
| 2026-04-19 | Initial version | — |
| 2026-04-19 | Recorded Phase 0–4: signals, candidates, scores, selected C1 | Frank + Claude |
